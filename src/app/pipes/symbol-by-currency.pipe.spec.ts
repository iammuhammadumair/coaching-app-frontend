import { SymbolByCurrencyPipe } from './symbol-by-currency.pipe';

describe('SymbolByCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolByCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
